pipeline {
    agent { docker { image 'php' } }
    stages {
        stage('build') {
            steps {
			echo "inside 123"
                sh 'php --version'
				echo "inside234"
            }
        }
    }
}